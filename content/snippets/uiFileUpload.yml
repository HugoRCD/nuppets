name: UFileUpload
description: Create an input element to upload files with Nuxt UI.
keyword: uploadFile
code: |
  ```vue
  <script setup lang="ts">
  const value = ref(null)
  </script>

  <template>
    <UFileUpload v-model="value" class="w-96 min-h-48" />
  </template>
  ```
tags:
  - nuxt-ui
  - nuxt
  - vue
  - component
